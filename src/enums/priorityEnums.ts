export enum PriorityDefault {
	lower = 'defer',
	peer = 'never',
	higher = 'stop',
	pauseDisplay = 'show',
}

export enum PriorityRule {
	defer = 'defer',
	never = 'never',
	stop = 'stop',
	pause = 'pause',
}
