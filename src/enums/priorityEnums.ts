export enum PriorityDefault {
	lower = 'defer',
	peer = 'defer',
	higher = 'stop',
	pauseDisplay = 'show',
}

export enum PriorityRule {
	defer = 'defer',
	never = 'never',
	stop = 'stop',
	pause = 'pause',
}
