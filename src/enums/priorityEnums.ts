export enum PriorityDefault {
	lower = 'defer',
	peer = 'stop',
	higher = 'pause',
	pauseDisplay = 'show',
}

export enum PriorityRule {
	defer = 'defer',
	never = 'never',
	stop = 'stop',
	pause = 'pause',
}
